name: debug_panel_example
description: "DebugPanel example."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  debug_panel:
    git:
      url: https://github.com/andyduke/flutter_debug_panel.git
      path: packages/debug_panel
  debug_panel_http:
    path: ../packages/debug_panel_http
  http_middleware:
    git:
      url: https://github.com/andyduke/dart_http_middleware.git
      path: package
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  logging: ^1.2.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
